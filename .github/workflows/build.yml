name: Build FAKE GPS DEB

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '**.mm'
      - '**.xm'
      - '**.plist'
      - '**.strings'
      - '**.json'
      - 'Makefile'
      - 'control'
      - 'scripts/**'
      - 'Resources/**'
      - '.github/workflows/build.yml'
  pull_request:
    branches: [main]

concurrency:
  group: fake-gps-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  THEOS: ${{ github.workspace }}/theos

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bash curl git make perl python3 build-essential fakeroot xz-utils unzip libplist-utils

      - name: Validate repository
        shell: bash
        run: bash scripts/validate.sh

      - name: Install Theos toolchain and SDK
        shell: bash
        run: |
          set -Eeuo pipefail
          rm -rf "$THEOS"
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
          test -x "$THEOS/toolchain/linux/iphone/bin/clang"
          test -n "$(find "$THEOS/sdks" -maxdepth 1 -type d -name '*.sdk' -print -quit)"
          "$THEOS/toolchain/linux/iphone/bin/clang" --version

      - name: Build rootless package
        shell: bash
        run: bash scripts/build.sh

      - name: Upload build files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: FAKE-GPS-${{ github.run_number }}
          path: |
            artifacts/*.deb
            artifacts/build.log
            artifacts/SHA256SUMS.txt
          if-no-files-found: warn
          retention-days: 30
